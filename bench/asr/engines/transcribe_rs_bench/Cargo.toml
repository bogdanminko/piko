[package]
name = "transcribe_rs_bench"
version = "0.1.0"
edition = "2024"

[dependencies]
transcribe-rs = { version = "0.3.11", features = ["whisper-cpp", "whisper-metal", "onnx"] }
serde_json = "1"

[profile.release]
lto = "thin"
